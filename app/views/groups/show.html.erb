
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<table>
  <thead>
    <tr>
      <th>Conciertos</th>
      <th>Ultimo Concierto</th>
      <th>Total Asistentes</th>
      <th>Maximo tiempo</th>
    </tr>
  </thead>

  <tbody>
    <% @concerts.each do |concert| %>
      <tr>
        <td><%#= concert.titulo %></td>
        <td><%#= concert.lanzamiento %></td>
        <td><%= concert.group.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Ver subpeliculas', category_movies_path(@category.id)%>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>